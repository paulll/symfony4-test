{% extends 'base.html.twig' %}

{% block title %}Author{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="/css/form-edit.css">
{% endblock %}

{% block body %}
    <h1>Author</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ author.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ author.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" role="button" href="{{ path('author_index') }}">Back</a>
    <a class="btn btn-primary" role="button" href="{{ path('author_edit', {'id': author.id}) }}">Edit</a>
    {{ include('author/_delete_form.html.twig') }}
{% endblock %}
